import { makeStyles } from "@material-ui/core";

const styles = makeStyles({
    container: {
        color: "#fff"
    },
    navbar: {
        background: "#fff",
    },
    brand: {
        alignSelf: "end"
    }
})

export default styles